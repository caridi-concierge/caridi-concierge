/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        fraunces: ["var(--font-fraunces)", "serif"],
        satoshi: ["var(--font-satoshi)", "sans-serif"],
      },
    },
  },
}